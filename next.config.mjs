/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.vecteezy.com",
      "media.licdn.com",
      "raw.githubusercontent.com",
      "github.com",
    ],
  },
};

export default nextConfig;
